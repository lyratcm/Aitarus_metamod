# only exists so that the techs can be taken from it
unfallen_empire_tech_country = {
    is_space_critter = yes
    destroys_starbases = no
    # blocks_pirate = yes
    observable = no
    government = no
    ai = {
        enabled = no
    }
    faction = {
        neutral = yes
        needs_border_access = no
        generate_borders = no
        needs_colony = no
        auto_delete = no
    }
    modules = {
        all_technology_module = {}
        exclusive_diplomacy_module = { contact_rule = on_action_only } # Nothing is allowed, but we need this for relationships.
    }
}
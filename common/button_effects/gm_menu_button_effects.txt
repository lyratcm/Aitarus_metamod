# disabled for now
gm_menu_button = {
	potential = {
		# will never be seen
		always = yes
		# has_country_flag = gm_is_host
	}
	allow = {
	}
	effect = {
		custom_tooltip = gm_menu_button_tooltip
		# print_scope_effect = yes
		hidden_effect = {
			gm_menu_scope_switch = yes
		}
	}
}